$("#filters").replaceWith(
  "<%= j render('pages/shared/filters', services: @services, filter: @filter) %>"
);

$("#results").replaceWith(
  "<%= j render('pages/shared/results', services: @services, filter: @filter) %>"
);

$("#form_search").replaceWith(
  "<%= j render('pages/shared/form_search', filter: @filter) %>"
);

$("#query").val('');