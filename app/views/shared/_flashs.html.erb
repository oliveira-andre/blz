  <% flash.each do |key, value| %>
    <div class="notification is-<%= key == 'error' ? 'danger' : key == 'success' ? 'success' : 'info'%> toast show">
      <button class="delete"></button>
      <%= value %>
    </div>
  <% end %>