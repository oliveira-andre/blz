<div class="devise">
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <section class="hero is-success is-fullheight">
      <div class="hero-body">
        <div class="container has-text-centered">
          <div class="column is-4 is-offset-4">
            <h3 class="title has-text-grey">Cadastre-se</h3>
            <p class="subtitle has-text-grey">Por favor realize o seu cadastro para prosseguir</p>
            <div class="box">
              <figure class="avatar">
                <img src="https://placehold.it/128x128">
              </figure>

              <%= render "devise/shared/error_messages", resource: resource %>

              <div class="field">
                <div class="control">
                  <%= f.text_field :name, autocomplete: "name", placeholder: "Nome", class: "input is-large" %>
                </div>
              </div>

              <div class="field">
                <div class="control">
                  <%= f.email_field :email, autocomplete: "email", placeholder: "Email", class: "input is-large" %>
                </div>
              </div>

              <div class="field">
                <div class="control">
                  <%= f.password_field :password, class: "input is-large", placeholder: "Senha" %>
                </div>
                <% if @minimum_password_length %>
                  <em>(<%= @minimum_password_length %> carácteres no mínimo)</em>
                <% end %><br/>
              </div>

              <div class="field">
                <div class="control">
                  <%= f.password_field :password_confirmation, class: "input is-large", placeholder: "Confirme sua senha" %>
                </div>
              </div>

              <div class="actions">
                <%= f.submit "Cadastrar", class: "button is-block is-info is-large is-fullwidth" %>
              </div>

            </div>
            <p class="has-text-grey">
              <%= render "devise/shared/links" %>
            </p>
          </div>
        </div>
      </div>
    </section>

  <% end %>

</div>